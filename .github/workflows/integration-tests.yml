name: integration-tests
on: [push, pull_request]

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  stopped:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: echo "Fork validation complete"
